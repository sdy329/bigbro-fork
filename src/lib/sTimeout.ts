export enum InputId {
    Duration = 'duration',
    Unit = 'unit',
    Reason = 'reason',
}

export enum ModalId {
    sTimeout = 'selfTimeout',
}

export enum UnitId {
    millisecond = 'ms',
    
}